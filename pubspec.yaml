name: my_billu_control
description: "My Billu Control - Admin Panel for Managing Client Subscriptions"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.12.1
  cloud_firestore: ^5.6.8
  firebase_auth: ^5.5.4
  google_sign_in: ^6.2.2
  google_fonts: ^6.2.1
  intl: ^0.20.2
  url_launcher: ^6.3.1
  local_auth: ^2.3.0
  shared_preferences: ^2.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
